# Include this directive in the HTTPS JupyterHub server block.
# JupyterLab uploads are JSON/base64 requests, so the request body is larger
# than the original file. Keep this aligned with DEPLOYMENT-SYNC.md.
client_max_body_size 250m;
